{
  while (true) {
    try {
      TBase<?,?> dto=take();
      if (dto == null) {
        continue;
      }
      send(dto);
    }
 catch (    Exception e) {
      logger.log(Level.WARNING,"Unexpected Error",e);
    }
  }
}
