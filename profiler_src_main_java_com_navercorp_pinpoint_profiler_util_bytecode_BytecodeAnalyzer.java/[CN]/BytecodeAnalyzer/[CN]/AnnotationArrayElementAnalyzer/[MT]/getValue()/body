{
  if (elements == null) {
    return new Object[0];
  }
  int size=elements.size();
  Object[] array=new Object[size];
  for (int i=0; i < size; i++) {
    array[i]=elements.get(i).value;
  }
  return array;
}
