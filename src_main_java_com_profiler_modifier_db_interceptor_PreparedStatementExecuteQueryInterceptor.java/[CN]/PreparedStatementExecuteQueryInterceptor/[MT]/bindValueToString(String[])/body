{
  if (temp == null) {
    return "";
  }
  StringBuilder sb=new StringBuilder();
  int end=temp.length - 1;
  for (int i=0; i < temp.length; i++) {
    sb.append(temp[i]);
    if (i < end) {
      sb.append(", ");
    }
  }
  return sb.toString();
}
