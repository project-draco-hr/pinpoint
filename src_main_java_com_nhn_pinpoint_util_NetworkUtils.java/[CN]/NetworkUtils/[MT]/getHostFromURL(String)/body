{
  try {
    URL u=new URL(url);
    String host=u.getHost();
    int port=u.getPort();
    if (port > 0) {
      return host + ":" + port;
    }
 else {
      return host;
    }
  }
 catch (  Exception e) {
    return null;
  }
}
