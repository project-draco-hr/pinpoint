{
  Assert.assertTrue(p.matcher("P1NPOINT???").find());
  Assert.assertTrue(p.matcher("P1NPOINT ").find());
  Assert.assertTrue(p.matcher("P1NPOINT+").find());
  Assert.assertTrue(p.matcher("PINPO+INT").find());
}
