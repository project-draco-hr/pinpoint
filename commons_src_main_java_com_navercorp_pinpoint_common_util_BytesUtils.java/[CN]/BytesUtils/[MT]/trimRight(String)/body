{
  if (string == null) {
    return null;
  }
  final int length=string.length();
  int index=length;
  while (string.charAt(index - 1) <= ' ') {
    index--;
  }
  if (index == length) {
    return string;
  }
 else {
    return string.substring(0,index);
  }
}
