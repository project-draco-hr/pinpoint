{
  HTable htable=(HTable)tablePool.getTable(tablename);
  try {
    return htable.get(get);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
 finally {
    closeHTable(htable);
  }
  return new Result[0];
}
