{
  try {
    for (int i=0; i < workCount; i++) {
      socketList=randomJob(socketList,address);
      try {
        Thread.sleep(1000);
      }
 catch (      InterruptedException e) {
      }
    }
  }
  finally {
    latch.countDown();
  }
}
