{
switch (field) {
case STACK_DEPTH:
    if (value == null) {
      unsetStackDepth();
    }
 else {
      setStackDepth((Integer)value);
    }
  break;
case STACK_FRAME:
if (value == null) {
  unsetStackFrame();
}
 else {
  setStackFrame((String)value);
}
break;
}
}
