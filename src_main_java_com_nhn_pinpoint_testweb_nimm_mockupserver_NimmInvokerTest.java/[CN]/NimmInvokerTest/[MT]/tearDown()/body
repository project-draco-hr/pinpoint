{
  if (sendSocket != null) {
    sendSocket.dispose();
  }
  if (receiveSocket != null) {
    receiveSocket.dispose();
  }
  if (invoker != null) {
    invoker.dispose();
    if (!invoker.isValid()) {
      throw new RuntimeException("invokoer is not valid.");
    }
  }
}
