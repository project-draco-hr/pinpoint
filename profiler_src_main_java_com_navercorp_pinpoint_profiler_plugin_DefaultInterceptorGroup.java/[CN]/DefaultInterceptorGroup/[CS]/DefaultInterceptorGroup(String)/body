{
  this.name=name;
  this.threadLocal=new ThreadLocal<InterceptorGroupTransaction>(){
    @Override protected InterceptorGroupTransaction initialValue(){
      return new DefaultInterceptorStack(name);
    }
  }
;
}
