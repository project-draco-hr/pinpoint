{
  Level topLevel=new Level("toplevel");
  levels.add(topLevel);
  for (int i=1; i < 10; i++) {
    Level prev=levels.get(i - 1);
    Level curr=new Level("level" + i);
    curr.setUp(prev);
    prev.setDown(curr);
    levels.add(curr);
  }
}
