{
  try {
    initServlets(port);
    server.start();
    logger.info("StatServer started");
    server.join();
  }
 catch (  Exception e) {
    logger.error("StatServer error : {}",e.toString());
  }
}
