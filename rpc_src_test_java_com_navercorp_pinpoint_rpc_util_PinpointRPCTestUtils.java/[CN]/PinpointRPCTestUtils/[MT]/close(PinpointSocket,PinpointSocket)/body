{
  if (socket != null) {
    socket.close();
  }
  if (sockets != null) {
    for (    PinpointSocket eachSocket : sockets) {
      if (eachSocket != null) {
        eachSocket.close();
      }
    }
  }
}
