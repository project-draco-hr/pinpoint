{
  if (client != null) {
    client.close();
  }
  if (clients != null) {
    for (    PinpointClient eachSocket : clients) {
      if (eachSocket != null) {
        eachSocket.close();
      }
    }
  }
}
