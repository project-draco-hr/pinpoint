{
  execute(tableName,new TableCallback(){
    @Override public Object doInTable(    HTableInterface htable) throws Throwable {
      htable.incrementColumnValue(rowName,familyName,qualifier,amount);
      return null;
    }
  }
);
}
