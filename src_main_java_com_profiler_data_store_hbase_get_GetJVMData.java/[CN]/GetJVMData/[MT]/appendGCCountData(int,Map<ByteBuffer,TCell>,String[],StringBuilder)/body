{
  String gc1Count=new String(columns.get(Converter.toByteBuffer(keyList[perColumnDataCount * 2 - 1])).getValue());
  String gc2Count=new String(columns.get(Converter.toByteBuffer(keyList[perColumnDataCount * 4 - 1])).getValue());
  gcCountData.append(gc1Count).append(",").append(gc2Count);
}
