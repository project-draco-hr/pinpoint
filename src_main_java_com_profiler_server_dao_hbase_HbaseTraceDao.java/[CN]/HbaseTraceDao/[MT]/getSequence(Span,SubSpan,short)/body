{
  if (sequence == -1) {
    if (!subSpan.isSetSequence()) {
      logger.warn("fist sequence number not exist {}",span);
    }
    return subSpan.getSequence();
  }
 else {
    return ++sequence;
  }
}
