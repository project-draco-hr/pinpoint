{
  this.name=name;
  this.threadLocal=new ThreadLocal<InterceptorGroupInvocation>(){
    @Override protected InterceptorGroupInvocation initialValue(){
      return new DefaultInterceptorGroupInvocation(name);
    }
  }
;
}
