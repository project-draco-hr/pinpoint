{
  if (CollectionUtils.isEmpty(values)) {
    return INTEGER_ZERO;
  }
  double avg=0;
  int cnt=1;
  for (  int value : values) {
    avg+=(value - avg) / cnt;
    ++cnt;
  }
  return (int)Math.round(avg);
}
