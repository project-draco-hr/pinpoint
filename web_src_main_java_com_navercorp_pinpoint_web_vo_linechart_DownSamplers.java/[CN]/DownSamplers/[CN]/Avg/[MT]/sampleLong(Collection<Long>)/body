{
  if (CollectionUtils.isEmpty(values)) {
    return LONG_ZERO;
  }
  double avg=0;
  int cnt=1;
  for (  long value : values) {
    avg+=(value - avg) / cnt;
    ++cnt;
  }
  return (long)Math.round(avg);
}
