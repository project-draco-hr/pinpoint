{
  verifier.verifyTraceCount(2);
  Method process=TBaseProcessor.class.getDeclaredMethod("process",TProtocol.class,TProtocol.class);
  verifier.verifyDiscreteTrace(root("THRIFT_SERVER","Thrift Server Invocation","com/navercorp/pinpoint/plugin/thrift/dto/EchoService/echo",SERVER_ADDRESS.getHostName() + ":" + SERVER_ADDRESS.getPort(),SERVER_ADDRESS.getHostName()),event("THRIFT_SERVER_INTERNAL",process));
  verifier.verifyTraceCount(0);
}
