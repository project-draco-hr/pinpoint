{
  FileReader reader=null;
  try {
    Properties prop=new Properties();
    reader=new FileReader(propertyName);
    prop.load(reader);
    return prop;
  }
  finally {
    if (reader != null) {
      try {
        reader.close();
      }
 catch (      IOException e) {
      }
    }
  }
}
