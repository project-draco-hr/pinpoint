{
  try {
    Future<Boolean> future=arcus.set("hippo:testkey",10,"Hello, Hippo.");
    future.get(1000L,TimeUnit.MILLISECONDS);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return "arcus";
}
