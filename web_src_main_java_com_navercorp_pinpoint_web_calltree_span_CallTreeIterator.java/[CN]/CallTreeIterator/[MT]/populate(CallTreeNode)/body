{
  nodes.add(node);
  index++;
  final long gap=getGap(node);
  node.setGap(gap);
  if (node.hasChild()) {
    populate(node.getChild());
  }
  if (node.hasSibling()) {
    populate(node.getSibling());
  }
}
