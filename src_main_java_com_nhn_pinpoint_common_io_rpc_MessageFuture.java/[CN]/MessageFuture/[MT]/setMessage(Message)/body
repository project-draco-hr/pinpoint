{
synchronized (this) {
    if (ready) {
      return false;
    }
    this.ready=true;
    this.message=message;
    if (waiters > 0) {
      notifyAll();
    }
  }
  cancelTimout();
  notifyListener();
  return true;
}
