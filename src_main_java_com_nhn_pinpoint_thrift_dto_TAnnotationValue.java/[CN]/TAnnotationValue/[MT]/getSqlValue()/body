{
  if (getSetField() == _Fields.SQL_VALUE) {
    return (TSqlValue)getFieldValue();
  }
 else {
    throw new RuntimeException("Cannot get field 'sqlValue' because union is currently set to " + getFieldDesc(getSetField()).name);
  }
}
