{
  context.addServiceType(ORACLE,new AnnotationKeyMatcher.ExactMatcher(AnnotationKey.ARGS0));
  context.addServiceType(ORACLE_EXECUTE_QUERY,new AnnotationKeyMatcher.ExactMatcher(AnnotationKey.ARGS0));
}
