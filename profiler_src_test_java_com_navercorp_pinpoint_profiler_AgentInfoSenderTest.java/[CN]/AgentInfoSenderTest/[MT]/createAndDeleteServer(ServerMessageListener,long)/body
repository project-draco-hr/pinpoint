{
  PinpointServerAcceptor server=null;
  try {
    server=createServerAcceptor(listner);
    Thread.sleep(waitTimeMillis);
  }
  finally {
    if (server != null) {
      server.close();
    }
  }
}
