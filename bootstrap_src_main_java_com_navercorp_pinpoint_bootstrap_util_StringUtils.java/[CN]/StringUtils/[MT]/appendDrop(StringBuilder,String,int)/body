{
  if (str == null) {
    return;
  }
  if (length < 0) {
    return;
  }
  if (str.length() > length) {
    builder.append(str.substring(0,length));
    appendDropMessage(builder,str.length());
  }
 else {
    builder.append(str);
  }
}
