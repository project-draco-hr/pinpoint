{
  if (classLoader == null) {
    return CONTINUE;
  }
  if (classLoader == agentLoader) {
    return SKIP;
  }
  if (className.startsWith("com/navercorp/pinpoint/")) {
    return SKIP;
  }
  return CONTINUE;
}
