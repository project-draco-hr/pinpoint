{
  Map<String,Long> map=local.get();
  if (map == null) {
    map=new HashMap<String,Long>(1);
    map.put(name,System.currentTimeMillis());
    local.set(map);
  }
 else {
    map.put(name,System.currentTimeMillis());
  }
}
