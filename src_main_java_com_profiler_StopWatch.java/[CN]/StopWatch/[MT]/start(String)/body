{
  Map<String,Long> map=local.get();
  if (map == null) {
    map=new HashMap<String,Long>(1);
    map.put(id,System.nanoTime());
    local.set(map);
  }
 else {
    map.put(id,System.nanoTime());
  }
}
