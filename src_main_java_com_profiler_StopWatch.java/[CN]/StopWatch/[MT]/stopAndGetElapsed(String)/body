{
  Map<String,Long> map=local.get();
  if (map == null) {
    return -1;
  }
 else {
    Long startTime=map.get(id);
    map.remove(id);
    if (startTime != null) {
      return System.nanoTime() - startTime;
    }
 else {
      return -1;
    }
  }
}
