{
  Map<String,Long> map=local.get();
  if (map == null) {
    return -1;
  }
 else {
    if (map.containsKey(id)) {
      return System.nanoTime() - map.get(id);
    }
 else {
      return -1;
    }
  }
}
