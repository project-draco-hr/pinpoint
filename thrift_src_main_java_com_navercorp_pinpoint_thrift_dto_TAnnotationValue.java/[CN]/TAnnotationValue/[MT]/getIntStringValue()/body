{
  if (getSetField() == _Fields.INT_STRING_VALUE) {
    return (TIntStringValue)getFieldValue();
  }
 else {
    throw new RuntimeException("Cannot get field 'intStringValue' because union is currently set to " + getFieldDesc(getSetField()).name);
  }
}
