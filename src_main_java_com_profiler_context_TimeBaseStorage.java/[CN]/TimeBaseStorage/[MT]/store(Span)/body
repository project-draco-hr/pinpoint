{
  if (discard) {
    limit=checkLimit(span);
    if (!limit) {
synchronized (this) {
        this.storage=null;
      }
      dataSender.send(span);
    }
 else {
      flushAll(span);
    }
  }
 else {
    flushAll(span);
  }
}
