{
  if (storage == null) {
    Logger logger=Logger.getLogger(this.getClass().getName());
    logger.fine("storage is null. direct send");
    return false;
  }
  storage.add(subSpan);
  return true;
}
