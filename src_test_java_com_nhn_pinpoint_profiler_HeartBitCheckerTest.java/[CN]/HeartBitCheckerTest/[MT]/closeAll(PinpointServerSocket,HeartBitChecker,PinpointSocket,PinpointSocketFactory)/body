{
  if (server != null) {
    server.close();
  }
  if (checker != null) {
    checker.stop();
  }
  if (socket != null) {
    socket.close();
  }
  if (factory != null) {
    factory.release();
  }
}
