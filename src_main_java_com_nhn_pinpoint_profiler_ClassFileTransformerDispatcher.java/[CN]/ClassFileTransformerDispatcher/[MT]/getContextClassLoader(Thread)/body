{
  try {
    return thread.getContextClassLoader();
  }
 catch (  Throwable th) {
    if (isDebug) {
      logger.debug("getContextClassLoader(). Caused:{}",th.getMessage(),th);
    }
    return null;
  }
}
