{
  if (bytes == null) {
    checkExpand(1);
    super.putSVar(NULL);
  }
 else {
    checkExpand(bytes.length + BytesUtils.VINT_MAX_SIZE);
    super.putSVar(bytes.length);
    super.put(bytes);
  }
}
