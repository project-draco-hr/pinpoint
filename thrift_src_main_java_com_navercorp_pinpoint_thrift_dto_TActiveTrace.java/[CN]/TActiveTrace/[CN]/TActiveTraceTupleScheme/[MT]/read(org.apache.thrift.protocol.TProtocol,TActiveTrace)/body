{
  TTupleProtocol iprot=(TTupleProtocol)prot;
  BitSet incoming=iprot.readBitSet(1);
  if (incoming.get(0)) {
    struct.histogram=new TActiveTraceHistogram();
    struct.histogram.read(iprot);
    struct.setHistogramIsSet(true);
  }
}
