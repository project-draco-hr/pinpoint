{
  final File[] libList=libDir.listFiles();
  if (libList == null) {
    return;
  }
  for (  File f : libList) {
    if (f.getName().endsWith(".jar")) {
      libraries.add(f.getAbsolutePath());
    }
  }
}
