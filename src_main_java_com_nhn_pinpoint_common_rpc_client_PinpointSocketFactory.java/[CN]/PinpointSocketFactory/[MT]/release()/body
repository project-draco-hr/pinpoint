{
synchronized (this) {
    if (released) {
      return;
    }
    released=true;
  }
  if (bootstrap != null) {
    bootstrap.releaseExternalResources();
  }
  List<Runnable> reconnecExevent=this.reconnector.shutdownNow();
}
