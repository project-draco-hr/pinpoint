{
synchronized (this) {
    if (released) {
      return;
    }
    released=true;
  }
  if (bootstrap != null) {
    bootstrap.releaseExternalResources();
  }
  List<Runnable> reconnectEvent=this.reconnector.shutdownNow();
}
