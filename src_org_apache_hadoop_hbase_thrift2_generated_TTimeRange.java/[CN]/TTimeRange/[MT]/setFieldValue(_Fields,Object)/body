{
switch (field) {
case MIN_STAMP:
    if (value == null) {
      unsetMinStamp();
    }
 else {
      setMinStamp((Long)value);
    }
  break;
case MAX_STAMP:
if (value == null) {
  unsetMaxStamp();
}
 else {
  setMaxStamp((Long)value);
}
break;
}
}
