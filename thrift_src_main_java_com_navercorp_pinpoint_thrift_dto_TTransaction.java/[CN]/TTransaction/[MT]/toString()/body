{
  StringBuilder sb=new StringBuilder("TTransaction(");
  boolean first=true;
  if (isSetTps()) {
    sb.append("tps:");
    sb.append(this.tps);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
