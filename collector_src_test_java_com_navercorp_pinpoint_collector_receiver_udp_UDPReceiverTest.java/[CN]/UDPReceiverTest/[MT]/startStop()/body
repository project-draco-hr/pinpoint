{
  try {
    DataReceiver receiver=new BaseUDPReceiver("test",new DispatchHandler(){
      @Override public void dispatchSendMessage(      TBase<?,?> tBase){
      }
      @Override public TBase dispatchRequestMessage(      TBase<?,?> tBase){
        return null;
      }
    }
,"127.0.0.1",10999,1024,1,10);
  }
 catch (  Exception e) {
    e.printStackTrace();
    Assert.fail(e.getMessage());
  }
}
