{
  if (names != null) {
    return names;
  }
  final String[] methodNames={"sync","syncAndReturnAll"};
  final Set<String> jedisMethodNames=JedisMethodNames.get();
  jedisMethodNames.addAll(Arrays.asList(methodNames));
  names=jedisMethodNames;
  return names;
}
