{
  Integer depth=scope.get();
  if (depth == null) {
    return NULL;
  }
 else   if (depth == ZERO) {
    scope.set(null);
    return ZERO;
  }
 else {
    scope.set(depth - 1);
    return depth;
  }
}
