{
  Integer depth=scope.get();
  if (depth == null) {
    return NULL;
  }
 else   if (ZERO.equals(depth)) {
    scope.set(null);
    return ZERO;
  }
 else {
    scope.set(depth - 1);
    return depth;
  }
}
