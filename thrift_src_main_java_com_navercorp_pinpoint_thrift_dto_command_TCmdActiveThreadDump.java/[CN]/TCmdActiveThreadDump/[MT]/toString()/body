{
  StringBuilder sb=new StringBuilder("TCmdActiveThreadDump(");
  boolean first=true;
  if (isSetExecTime()) {
    sb.append("execTime:");
    sb.append(this.execTime);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
