{
  int size=70000;
  DatagramPacket packet1=newDatagramPacket(size);
  try {
    sender.send(packet1);
    Assert.fail("???????????? ???????????? ??????.");
  }
 catch (  IOException e) {
    logger.log(Level.INFO,"???????????? ?????? ??????. " + e.getMessage(),e);
  }
}
