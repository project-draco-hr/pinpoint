{
  short applicationNameLength=BytesUtils.bytesToShort(bytes,0);
  byte[] temp=new byte[applicationNameLength];
  System.arraycopy(bytes,2,temp,0,applicationNameLength);
  return BytesUtils.toString(temp);
}
