{
switch (field) {
case ROUTE_RESULT:
    if (value == null) {
      unsetRouteResult();
    }
 else {
      setRouteResult((TRouteResult)value);
    }
  break;
case PAYLOAD:
if (value == null) {
  unsetPayload();
}
 else {
  setPayload((ByteBuffer)value);
}
break;
case MESSAGE:
if (value == null) {
unsetMessage();
}
 else {
setMessage((String)value);
}
break;
}
}
