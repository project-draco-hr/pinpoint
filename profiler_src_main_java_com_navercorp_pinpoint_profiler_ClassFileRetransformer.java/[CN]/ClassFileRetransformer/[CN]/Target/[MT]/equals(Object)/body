{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  Target other=(Target)obj;
  return (this.loader == other.loader) && (this.className.equals(other.className));
}
