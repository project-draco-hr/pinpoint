{
  while (true) {
    try {
      Object dto=take();
      if (dto == null) {
        continue;
      }
      send0(dto);
    }
 catch (    Throwable e) {
      logger.log(Level.WARNING,"Unexpected Error Cause:" + e.getMessage(),e);
    }
  }
}
