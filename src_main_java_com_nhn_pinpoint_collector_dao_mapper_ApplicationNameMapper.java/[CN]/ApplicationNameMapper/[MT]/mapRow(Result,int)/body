{
  KeyValue[] raw=result.raw();
  if (raw.length == 0) {
    return null;
  }
  String[] ret=new String[raw.length];
  int index=0;
  for (  KeyValue kv : raw) {
    ret[index++]=new String(kv.getQualifier(),"UTF-8");
  }
  return ret[0];
}
