{
  ts.restart();
  Thread.sleep(5000);
  ZooKeeper zookeeper=new ZooKeeper(zookeeperAddress,5000,null);
  getNodeAndCompareContents(zookeeper);
  if (zookeeper != null) {
    zookeeper.close();
  }
}
