{
  if (classNames == null) {
    return REJECT;
  }
  for (  String className : classNames) {
    if (className != null && className.equals(clazz.getName())) {
      return ACCEPT;
    }
  }
  return REJECT;
}
