{
  if (args == null) {
    return "null";
  }
  if (args.length == 2) {
    return StringUtils.drop(getClassName(args[1]));
  }
 else   if (args.length == 3) {
    return StringUtils.drop(getClassName(args[1]));
  }
  return "error";
}
