{
  if (min > value) {
    min=value;
  }
  if (max < value) {
    max=value;
  }
  int slot=value / resolution;
  Integer org=counts.get(slot);
  if (org == null) {
    counts.put(slot,1);
  }
 else {
    counts.put(slot,++org);
  }
  sampleCount++;
}
