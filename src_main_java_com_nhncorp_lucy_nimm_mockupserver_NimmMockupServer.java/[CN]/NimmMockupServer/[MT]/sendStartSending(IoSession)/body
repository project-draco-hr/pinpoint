{
  NimmControlRecord startSending=NimmControlRecord.createControlMessage(this.serverNimmAddress,this.representAddress.get(ioSession),NimmControlRecord.ControlCode.START_SENDING,null);
  ioSession.write(startSending);
}
