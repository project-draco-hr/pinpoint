{
  if (annotations == null) {
    return false;
  }
  for (  BytecodeAnnotation annotation : annotations) {
    if (annotation.getDescriptor().equals(descriptor)) {
      return true;
    }
  }
  return false;
}
