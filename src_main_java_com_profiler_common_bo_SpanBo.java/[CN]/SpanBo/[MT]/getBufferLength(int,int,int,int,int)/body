{
  int size=a + b + c+ d+ e;
  size+=1 + 1 + 1+ 1+ 1+ VERSION_SIZE;
  size+=PARENTSPANID + SERVICETYPE + AGENTIDENTIFIER+ EXCEPTION_SIZE;
  if (flag != 0) {
    size+=FLAG;
  }
  size+=12;
  return size;
}
