{
  if (arg == null) {
    return "null";
  }
 else {
    if (arg instanceof String) {
      return (String)arg;
    }
 else     if (isSimpleType(arg)) {
      return arg.toString();
    }
 else {
      return arg.getClass().getSimpleName();
    }
  }
}
